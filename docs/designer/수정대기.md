# 수정할 내용(md viewer)
- 더 많은 컴포넌트 등록
- 컴포넌트 더블 클릭해서 label 변경 기능
- 최소크기, 하단 스크롤바
- 차트는 고정된 비율이 아니라 다양한 비율 가능해야 함(높이 핸들 추가), 제목 표시 가능해야함
- 트리도 핸들로 높이 조절 가능해야 함


# 나중에 수정할 것
- 입력 창을 가지는 것들을 묶어서 형태 변경이 가능하도록 할것
- 사용자 프로퍼티, 쓰는 값만 모은 프로퍼티
  1. 지금 프로퍼티 창의 형태
  


# 완료
- 아웃라인은 위에 햄버그 아이콘으로 접을 수 있게 해줘.
- delete / insert
- 드래그할 때 컨테이너 좌우에 플래이스 홀더가 너무 빨리 나와. 나는 안에 넣고 싶은데 너무 빨리 나와서 위치가 자꾸 흔들림. 컨테이너 옆의 플래이스홀더는 좀 늦게 나오면 좋겠다.  
- 가장 왼쪽에 잇는 properties, live preview 버튼은 가장 오른쪽에 있는 side-panel 위에 탭 형식으로 선택하게 하자.
- view json (edit 가능) - UI 파일 업로드 기능
- 테이블 Header Items 가 안되는 문제
- 컴포넌트 아래 핸들(draggable=true)을 달아서 높이 설정 기능
- 멀티 선택 기능 탐색 후 기능 추가 (미완성)
- 마진/패딩 반으로 줄이자.
- 마크다운에 랜더링 되는 플러그인(mermaid 처럼), 수정 모듈도 있으면 좋겠다.
- 컴포넌트 스페이싱 조절



부재료 배차 --> 속도 느림
n 인터지스 배차 --> 문제 없음
A0 동 PDA 연결은 안됨(업체 호출), 나머지 PDA 는 잘됨

# simple properties 패널 변경 요청
## 전체에서 높이 항목은 안보여도 됨
- 예외: Spacer 

## 높이 핸들 확인(전체 조사해서 없으면 추가해야 함)
- Input :  Text area, File picker
- Selection : Checkbox Group, Radio group
- Pregentation : 전체
- Containers : 전체

## Imave View 항목 추가 요청
- Image Source, Alternative text

## Table 항목 추가 요청
 - Header items 항목 추가

## 차트 항목 추가 요청
 - 제목

# 오류 수정 요청
- 더블 클릭 시 라벨 수정이 안됨, 직접 실행해서 확인 요청




 form-js-designer 모노레포에서 진행 중이던 "Simple/Full Properties Panel" 기능 작업을 이어서 한다.

  ## 현재 상태
  - Worktree: /Users/jji/project/form-js-designer-simple-props
  - Branch: feat/simple-props-panel (develop에서 분기)
  - 패키지 매니저: npm only (메모리 정책: pnpm install 금지)
  - 마지막 commit: dc6e45d  fix(props-panel): pass through option-source groups + improve reflow
  - 호스트 단위 테스트 369/369 PASS, designer-core 9/9 PASS, tsc baseline 42 errors (변동 없음)
  - 공식 plan 파일: /Users/jji/.claude/plans/file-users-jji-project-form-js-designer-sunny-meteor.md (subagent-driven-development 양식)

  ## 완료된 Task (브랜치 commit log 기준)
  1. simpleModeWhitelist 데이터 + helper (designer-core)            commit 99869ff
  2. I18nSimpleWidget (designer-core, opt-in, BUILTIN_WIDGETS 미변경) commit 39078b6
  3. PropsPanelService.getGroups(field, opts) 자체 패널 simple 필터  commit 1476bb9
     ※ 자체 그룹 id를 'designer-props' → 'designer-custom-props' 로 통일
  4. form-js native panel filter provider 등록 (host)                commit 1fcfb8b → 500 priority로 fix in fd1e473
  5. PropsPanelModeToggle 컴포넌트 + CSS                              commit 5f7a032
  6. PropsPanelContainer mode prop + 재계산                            commit 467f07a
  7. App.tsx 통합 (panelMode state, sessionStorage, toggle, native reflow) commit 0153f6a
  8. 브라우저 통합 검증 (Playwright visible) — 진행 중
     - textfield Simple 모드: 단일 'simple-merged' 그룹, [label, defaultValue, columns, required] 평탄 리스트로 렌더 ✓
     - 토글 클릭으로 Simple↔Full 전환 시 setMode + pp._render() reflow 정상 ✓
     - Full 모드 native: General/Condition/Layout/Appearance/Validation/CustomProperties 모두 노출 ✓

  ## 사용자 추가 요구 (반영됨)
  - Simple 모드 native 그룹들을 단일 'simple-merged' 그룹으로 평탄화 (헤더 없이 항상 열림 — CSS !important로 헤더 숨김 + entries display:block)
  - checklist/radio/select/taglist의 옵션 편집 영역(group-staticOptions, group-valuesSource) PASSTHROUGH 처리

  ## 알려진 follow-up (다음 세션 시작점)
  1. **staticOptions timing 이슈**: importSchema + selection.set을 같은 micro-task에 호출하면 form-js default provider가 group-staticOptions를 entries=[]로 빌드하는 첫 reflow가 발생, 그 이후 staticOptions 항목이 화면에 안 보임 (Full 모드는 정상).
  재현·root-cause 조사 필요.
  2. **첫 페이지 로드 시 sessionStorage='full' 인데도 native가 simple-merged로 그려지는 timing 이슈**: services.propsPanel state가 채워지는 시점과 useEffect 실행 순서. 토글 클릭하면 정상 동작.
  3. **Card / Modal / Tabs / Tree / Tab panel** 등 우리 커스텀 6종을 캔버스에서 선택했을 때 Simple 모드 자체 PropsPanelContainer 동작 확인.
     - 자체 PropsPanelContainer는 designer-custom-props/designer-layout 그룹 헤더가 그대로 보임. 사용자가 native만 평탄화로 만족했는지, 자체 패널도 같은 정책으로 합칠지 확인 필요.
  4. **i18n widget 시각 비교**: Simple 모드는 ko 한 줄 input, Full 모드는 i18n key + ko 두 줄. 실제 화면에서 카드 header / chartPlaceholder title 등으로 검증.
  5. **Task 9 (vscode-extension 동기화 + tiptap 회귀)** 미착수:
     - packages/designer-vscode-extension/src/editor/propsPanel/PropsPanelService.ts 에 host와 동일한 simpleRegistry/setMode/native filter provider 추가
     - customEditor.ts에 panelMode state + 토글 mount + reflow trigger
     - VSIX 패키징(unscoped name 패턴, 메모리 'project_vsix_packaging_unscoped' 참고) → VS Code 설치 후 .form-js-designer 파일에서 동일 시나리오 재실행
     - tiptap 패키지(designer-tiptap)는 ProseMirror Node라 화이트리스트 무관. tiptap viewer/editor에서 form-js block mount 시 회귀 없음만 확인.

  ## 진행 방식
  - superpowers:subagent-driven-development 워크플로 그대로 (메모리: superpowers 기본 = subagent-driven, 옵션 묻지 말고 진행 의사만 확인).
  - 다음 turn에서: dev 서버 재기동 → 위 follow-up 1~4 우선순위로 처리 → Task 9 dispatch.
  - 브라우저 검증은 plugin_playwright(visible) 사용. 시작 전 `pkill -f "Google Chrome.*playwright"` 선행 (메모리 'feedback_playwright_mcp_env' 정책).

  ## 메모리 주의사항 (관련된 것만)
  - E2E·실제 브라우저 검증 의무: headless만으로 완료 보고 금지.
  - form-js editor host 필수 의존성 (form-js-base.css, vite define.global, config.icon fallback 등). 이미 worktree에서 동작 확인됨.
  - npm canonical, pnpm install 금지.

  ## 시작 시 첫 명령어 권장
  ```bash
  cd /Users/jji/project/form-js-designer-simple-props
  git status && git log --oneline -10
  cd packages/designer-editor-host && npm run dev &
  sleep 5 && curl -sf http://localhost:5173 && echo OK